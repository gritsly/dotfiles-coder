# Configures dotfiles for codespaces


Uses dotfiles.symlink script to create dotfiles.

See https://github.com/linuskendall/dotfiles/

Expects the user to have a $GITHUB_USERNAME/dotfiles repo to load from. THis repo needs to be public. 
